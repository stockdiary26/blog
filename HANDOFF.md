# 이어서 작업할 내용

## 사용자 의도와 현재 상태

- 개인 블로그 parkmikyoung를 카페24 호스팅에 배포하는 작업입니다.
- 호스팅: 뉴아우토반호스팅 일반형, PHP 8.4 / MariaDB 10.x, 기본 도메인 miky2126.mycafe24.com, SSL 정상(관리 화면 확인).
- Node.js 서버를 PHP로 전환했습니다. `cafe24/`에 업로드할 파일이 있습니다.
- 실제 카페24 FTP 업로드 및 운영 환경 검증은 **아직 하지 않았습니다**. FTP/SFTP 접속 방법을 확인하는 단계입니다.
- PHP 코드는 7.4+ 문법으로 작성했고 PHP 8.3.33에서 로컬 구문/통합 검사했습니다. PHP 8.4 카페24 환경에서는 별도 확인이 필요합니다.
- 현재 PHP 배포 데이터는 글 7개입니다. 빈 이미지 주소 블록은 배포 스냅샷에서 제외했습니다. 원본 로컬 데이터는 별도로 남겼습니다.
- 실제 관리자 계정 파일, FTP 비밀번호, 초기 설정 코드는 Git에 포함하지 않습니다.

## 완료한 기능

- 문단과 이미지 블록, 이미지 URL 입력/미리보기, 첫 이미지 썸네일, 업로드 파일 편집 시 유지.
- 썸네일 높이 160px -> 190px.
- Admin 내부 비밀번호 변경, 변경 후 다른 세션 해제.
- PHP API: 로그인/계정 설정/로그아웃/비밀번호, 메뉴·카테고리·글 CRUD.
- PHP 저장: JSON 파일, 파일 잠금/원자 교체, PHP 비밀번호 해시/세션.
- PHP 보호: _private HTTP 차단, 업로드 PHP 실행 차단, 최초 계정 설정 코드, Origin 검사, 로그인 시도 제한.
- PHP 링크 미리보기: 제한된 외부 다운로드(cURL 필요), 실패해도 글 저장 가능.
- `/api/capabilities`로 이미지 URL 기능 지원 확인. 이전 서버에 저장하여 주소가 사라지는 일을 저장 전 차단.

## 검증 내역

격리된 데이터로 PHP API 계정 설정/로그인/로그아웃, 비밀번호 변경, 기존 세션 해제, 외부 Origin 차단, 계정 파일 접근 차단, 글 생성/수정/삭제, 업로드 유지/삭제, 이미지 URL 썸네일, 잘못된 수정 입력 시 기존 데이터 유지, 메뉴/카테고리 CRUD 및 삭제 제한을 검사했습니다.
브라우저에서 실제 기존 글 수정 -> 이미지 주소 추가 -> 저장 -> 새로고침 -> 이미지 표시를 확인했습니다. Diary/Archive/하위 필터/브라우저 뒤로가기도 확인했습니다.
ZIP의 .htaccess 포함 여부, 데이터/이미지 참조, 관리자 계정 파일 제외 여부를 확인했습니다.
카페24 Apache .htaccess 적용, 실제 FTP 업로드, PHP 8.4 실환경, 모바일 실기기 검증은 남아 있습니다.

## 다음 단계

1. 사용자와 FTP/SFTP 접속 방법을 확인합니다. 비밀번호는 채팅에 요청하지 않습니다.
2. 기존 www를 백업하고 준비한 PHP 파일을 업로드합니다.
3. /api/capabilities가 cafe24-php-v1인지, /_private/posts.json이 403인지 실제 서버에서 확인합니다.
4. 사용자가 초기 설정 코드와 새 관리자 비밀번호를 직접 입력해 계정을 만듭니다.
5. 별도 테스트 글로 생성/수정/이미지/삭제 및 로그인/메뉴를 검증하고 데스크탑과 서버 백업을 정리합니다.

## 유지보수 주의

AGENTS.md의 모듈 소유권을 따릅니다. 실제 글은 삭제 테스트에 쓰지 않습니다.
로컬 서버를 Codex exec에서 띄우면 응답 종료 후 프로세스가 종료되는 문제가 반복됐습니다. 사용자가 직접 연 터미널을 유지하거나 호스팅 PHP 환경을 사용하세요. 실행 직후 HTTP 200만으로 장기 유지/저장 성공을 단정하지 마세요.
Git push가 이 실행 환경에서 Windows TLS/인증 오류로 실패한 적이 있습니다. 사용자 PowerShell에서 push 성공한 커밋은 88ab93e였습니다. 이후 PHP 전환 커밋의 원격 업로드 상태는 실제 확인해야 합니다.
개인 Windows 절대 경로가 담긴 옛 실행 파일 대신 DESKTOP-START.md와 상대 경로 scripts/package-cafe24.ps1을 사용합니다.
